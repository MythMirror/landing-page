// This file is kept for import compatibility but the actual loader
// is now AppLoader (no WebGL dependency).
// PageWrapper loads AppLoader directly.
export default function Loader3D() {
  return null;
}
